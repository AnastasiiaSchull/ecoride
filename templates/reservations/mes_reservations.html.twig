{% extends 'base.html.twig' %}

{% block title %}Mes réservations{% endblock %}

{% block body %}

<div class="container py-5">

<h2 class="mb-4">
    <i class="bi bi-calendar-check me-2"></i>
    Mes réservations
</h2>

{% if reservations is empty %}

    <div class="alert alert-info">
        Vous n'avez aucune réservation.
    </div>

{% else %}

    <div class="row g-4">

        {% for r in reservations %}

            <div class="col-12 col-lg-8 mx-auto">

                <div class="card shadow-sm h-100">

                    <div class="card-body">

                        <h5 class="card-title">
                            {{  r.trajet.villeDepart }}
                            <i class="bi bi-arrow-right mx-1"></i>
                            {{ r.trajet.villeArrivee }}
                        </h5>

                        <p class="text-muted mb-2">
                            <i class="bi bi-clock"></i>
                            {{ r.trajet.dateDepart|date('d/m/Y H:i') }}
                        </p>

                        <p>
                            Statut :
                            {% if r.statut == 'confirmee' %}
                                <span class="badge bg-success">
                                    Confirmée
                                </span>
                            {% elseif r.statut == 'en_attente' %}
                                <span class="badge bg-warning text-dark">
                                    En attente
                                </span>
                            {% elseif r.statut == 'annulee' %}
                                <span class="badge bg-danger">
                                    Annulée
                                </span>
                            {% else %}
                                <span class="badge bg-secondary">
                                    {{ r.statut }}
                                </span>
                            {% endif %}
                        </p>

                    </div>

                    {% if r.peut_avis|default(0) == 1 %}

                        <div class="card-footer bg-white border-0">

                            <a href="{{ path('app_avis_nouveau', {
                                reservation_id: r.id
                            }) }}"
                               class="btn btn-primary w-100">

                                <i class="bi bi-star-fill me-2"></i>
                                Écrire un avis

                            </a>

                        </div>

                    {% endif %}

                </div>

            </div>

        {% endfor %}

    </div>

{% endif %}


</div>
<div class="d-flex justify-content-center mt-4">
    <a href="{{ path('home') }}" class="btn btn-outline-secondary">
        ← Retour à l'accueil
    </a>
</div>
{% endblock %}
